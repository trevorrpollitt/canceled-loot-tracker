{
  "name": "loot-tracker-monorepo",
  "private": true,
  "workspaces": [
    "src/web/client",
    "src/web/server"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspaces"
  }
}